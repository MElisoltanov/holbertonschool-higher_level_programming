#!/usr/bin/python3
"""Defines an empty class Rectangle."""
# Les trois guillemets (""") ouvrent une chaîne de caractères multilignes,
# appelée docstring. Elle sert à documenter le fichier ou le module.
# Ici, elle explique que ce fichier définit une classe Rectangle vide.
# Les docstrings sont utilisées par les outils de documentation Python.

class Rectangle:
    # Le mot-clé 'class' permet de définir une nouvelle classe en Python.
    # Une classe est un plan (ou modèle) pour créer des objets.
    # Ici, le nom de la classe est 'Rectangle'.
    # Par convention, le nom d'une classe commence par une majuscule.
    # Les deux points (:) indiquent le début du bloc de code de la classe.

    """Empty class Rectangle."""
    # Ceci est une docstring pour la classe Rectangle.
    # Elle explique que la classe est vide pour l'instant.
    # Les docstrings de classe sont utiles pour décrire le but de la classe.

    pass
    # Le mot-clé 'pass' indique que le bloc de code est vide.
    # Ici, cela signifie que la classe Rectangle ne contient aucune
    # méthode ni attribut pour l'instant.
    # 'pass' est utilisé comme un espace réservé pour éviter une erreur
    # de syntaxe lorsque le bloc doit rester vide.
